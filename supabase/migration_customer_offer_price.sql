-- ============================================================
-- Migration: إضافة إمكانية اقتراح سعر من العميل نفسه للطلب
-- شغّل السطر ده مرة واحدة بس من Supabase SQL Editor
-- ============================================================

alter table public.orders
  add column if not exists customer_offer_price numeric(10,2);

-- ملاحظة: مفيش داعي لتعديل RLS policies، لأن العميل أصلاً
-- عنده صلاحية تعديل طلباته الخاصة (participants update orders)
-- وده بيغطي تحديث العمود الجديد ده تلقائيًا.
